# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=jcroql

version: 2

before:
  hooks:
    - go mod tidy
    - go generate

builds:
  - id: "Build for all platforms"
    env:
      - CGO_ENABLED=0
    # ldflags: -s -w
    goos:
      - linux
      # - darwin
      - windows
    goarch:
      - amd64
      # - arm
      # - arm64
    goarm:
      - "7"
      - "6"
    ignore:
      # Ignore arm for windows
      - goos: windows
        goarch: arm
      - goos: linux
        goarch: arm
      # Ignore arm v4 for macos
      - goos: darwin
        goarch: arm
        goarm: "6"
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{ .CommitDate }} -X main.builtBy=goreleaser -X main.treeState={{ .IsGitDirty }}

release:
  prerelease: auto

dockers:
  # Stable release — pushed only for non-prerelease tags (e.g. v1.0.0)
  - image_templates:
      - "mchauge/mediamtx-path-viewer:stable"
      - "mchauge/mediamtx-path-viewer:v{{ .Version }}"
    skip_push: "{{ if .Prerelease }}true{{ else }}false{{ end }}"
    dockerfile: dockerfile
    goos: linux
    goarch: amd64
    use: docker
    build_flag_templates:
      - "--build-arg=VERSION={{ .Version }}"

  # Beta/pre-release — pushed only for prerelease tags (e.g. v1.0.0-beta, v1.0.0-pre)
  - image_templates:
      - "mchauge/mediamtx-path-viewer:beta"
    skip_push: "{{ if .Prerelease }}false{{ else }}true{{ end }}"
    dockerfile: dockerfile
    goos: linux
    goarch: amd64
    use: docker
    build_flag_templates:
      - "--build-arg=VERSION={{ .Version }}"
